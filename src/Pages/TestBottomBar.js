import React, {  } from 'react';
import BottomBar from '../Components/Bottombar';

const TestBottomBar = () => {   
    return (
        <div>
            <BottomBar/>
        </div>
    );
};

export default TestBottomBar;
import React, {  } from 'react';
import BottomBar from '../../Components/Bottombar';

const Profile = () => {   
    return (
        <>
            <BottomBar/>
        </>
    );
};

export default Profile;